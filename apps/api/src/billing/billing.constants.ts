/** Duração do período de teste, em dias (decisão de produto). */
export const TRIAL_DAYS = 30;

/** Janela de tolerância em past_due antes de bloquear o acesso, em dias. */
export const GRACE_DAYS = 3;
